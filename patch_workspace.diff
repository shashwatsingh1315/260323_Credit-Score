--- src/app/cases/[id]/CaseWorkspace.tsx
+++ src/app/cases/[id]/CaseWorkspace.tsx
@@ -214,7 +214,7 @@
             </div>

           {pendingRevision && pendingRevision.status === 'requested' && isRm && (
-            <Card className="mb-4 bg-muted/20 border-border print:hidden">
+            <Card className="mb-4 bg-card border-border print:hidden">
               <CardHeader className="pb-2">
                 <CardTitle className="text-[15px] flex items-center gap-2 text-warning">
                   <AlertCircle size={16} /> Revision Requested ({pendingRevision.stage_from})
@@ -238,7 +238,7 @@
           )}

           {c.status === 'Draft' && pendingRevision && pendingRevision.status === 'resolved' && (
-            <Card className="mb-4 bg-muted/20 border-warning print:hidden">
+            <Card className="mb-4 bg-card border-warning print:hidden">
               <CardHeader className="pb-2">
                 <CardTitle className="text-[15px] text-warning flex items-center gap-2">
                   <AlertCircle size={16} /> Revision Submitted
@@ -262,7 +262,7 @@
           )}

           {c.status === 'Approved_Conditionally' && isRm && (
-            <Card className="mb-4 bg-card border-primary print:hidden">
+            <Card className="mb-4 bg-card border-primary/30 print:hidden">
               <CardHeader className="pb-2">
                 <CardTitle className="text-[15px] text-primary flex items-center gap-2">
                   <CheckCircle2 size={16} /> Conditional Approval Action Required
@@ -355,7 +355,7 @@

           {/* Summary Cards */}
           <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
             <Card>
-              <CardHeader className="pb-3 bg-card"><CardTitle className="text-base text-foreground">Party History & Exposure</CardTitle></CardHeader>
+              <CardHeader className="pb-3 border-b border-border/50"><CardTitle className="text-base text-foreground">Party History & Exposure</CardTitle></CardHeader>
               <CardContent className="pt-4 space-y-4">
                 <div className="flex flex-col gap-1">
                   <span className="text-sm text-muted-foreground font-medium">Customer: {c.customer_party?.legal_name}</span>
@@ -424,7 +424,7 @@
             </Card>

             {c.status === 'Completed' && isRm && c.outcome_data && !c.outcome_data.deal_happened && (
-              <Card className="col-span-2 bg-card border-border print:hidden">
+              <Card className="col-span-2 bg-card border-border/50 print:hidden">
                 <CardHeader className="pb-2">
                   <CardTitle className="text-base text-foreground flex items-center gap-2">
                     <FileText size={16} /> Close-Out Outcome
