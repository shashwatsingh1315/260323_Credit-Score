## 2024-05-20 - Initial Setup **Learning:** The system has many scattered arbitrary text and layout values using Tailwind brackets (e.g. text-[10px], w-[200px]). The CSS variable palette is also duplicated with legacy hex code variables. **Action:** Need to document target aesthetic in DESIGN_STYLE.md and migrate all raw pixel/hex usage to strict token definitions like text-tiny, or standard w-48 spacing increments.
