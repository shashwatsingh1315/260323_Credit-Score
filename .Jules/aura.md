## YYYY-MM-DD - [Legacy System Cleanup] **Learning:** The current UI has remnants of arbitrary spacing (e.g. `w-[200px]`, `w-[440px]`, `text-[11px]`, `text-[10px]`) and legacy hardcoded inline styles. This causes structural conflicts with the semantic token architecture and prevents uniform dark-mode / scaling updates. **Action:** Introduced `text-micro` and reinforced `text-tiny` in tailwind config. Systematically replacing arbitrary magic numbers with Tailwind base-4/8 spacing values (e.g., `w-48`, `w-64`, `max-w-md`) and swapping inline styles for semantic tailwind classes globally.
