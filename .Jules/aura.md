## 2024-07-16 - Initial Token Audit **Learning:** Found extensive use of legacy hex-based variable names (like var(--bg-primary), var(--text-muted)) across CSS Modules which clashes with standard Tailwind HSL tokens mapped in globals.css. **Action:** Will perform a global replacement of legacy css variables with correct Tailwind HSL semantic tokens (like hsl(var(--background)), hsl(var(--muted-foreground))).
