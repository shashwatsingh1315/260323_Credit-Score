## 2025-05-22 - Legacy Hex Custom Properties Cleanup **Learning:** Found multiple usages of legacy custom properties like `--bg-primary`, `--bg-secondary`, `--bg-tertiary`, `--text-primary`, `--text-secondary`, `--text-muted`, `--border-primary`, `--border-color`, and `--danger` in `src/**/*.module.css` and `globals.css` that need to be migrated to the new HSL tailwind tokens: `hsl(var(--background))`, `hsl(var(--card))`, `hsl(var(--muted))`, `hsl(var(--foreground))`, `hsl(var(--muted-foreground))`, `hsl(var(--border))`, and `hsl(var(--destructive))`. **Action:** Search for and replace these deprecated custom properties in all CSS modules. Ensure `DESIGN_STYLE.md` accurately documents this mapping.
