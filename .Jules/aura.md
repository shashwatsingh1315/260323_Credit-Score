## 2024-05-18 - [Tokenizing Magic Dimensions] **Learning:** Found widespread usage of arbitrary dimensions (e.g., `w-[200px]`, `h-[50vh]`, `min-h-[80px]`) that break the 4px/8px scalable grid system. Also found `left-[50%]` / `translate-x-[-50%]` instead of standard fractional classes like `left-1/2` / `-translate-x-1/2`. **Action:** Will map these legacy bracket-notations to standard Tailwind scaling tokens and add `text-tiny` where `text-[10px]` or `text-[11px]` are hardcoded.
