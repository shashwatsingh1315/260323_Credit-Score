## 2025-05-25 - [Base Spacing Standard] **Learning:** Found arbitrary inline paddings (`px-[17px]`, padding: 22px etc from examples, and actual inconsistent margins like `mt-2` which is tailwind but sometimes we see arbitrary `mt-0.5` or `min-h-[80px]` etc. We must strictly use base-4/8 spacing tokens and sizing tokens. **Action:** Apply standard tailwind spacing classes and avoid arbitrary brackets for sizing.
