## 2024-05-05 - [Initial Exploration] **Learning:** Identified numerous instances of arbitrary CSS variables (`w-[...]`, `h-[...]`, `text-[...]`) across the codebase, contradicting the defined token scale. Additionally, found `min-h-[80px]` and `max-h-[400px]` which should be replaced with `min-h-20` and `max-h-96` respectively. **Action:** Apply systematic replacement of magic numbers to tokenized spacing, sizing, and typography across `src/components` and `src/app`.
## 2024-05-05 - [CSS Variable Tokens] **Learning:** Despite Tailwind being set up and `globals.css` declaring standard semantic HSL tokens (`--background`, `--foreground`), legacy hex tokens (`--bg-primary`, `--bg-secondary`, `--bg-tertiary`, `--text-primary`, `--text-secondary`, `--text-muted`, `--border-primary`, `--border-color`) are still extensively used inside many `.module.css` files. In `globals.css`, global styles for `select`, `input`, and `textarea` still reference these legacy hex variables instead of semantic HSL tokens. **Action:** Refactor all `.module.css` references and `globals.css` base styles from legacy hex variables to standard Tailwind CSS semantic variables, e.g., `var(--bg-tertiary)` to `hsl(var(--muted))`.
## 2024-05-05 - [Initial Token Setup] **Learning:** Wrote `DESIGN_STYLE.md` to establish the high-end baseline. Discovered that the codebase currently suffers from inconsistent sizing (`w-[200px]`, `min-h-[80px]`) and legacy color usage (`--bg-primary`). **Action:** Applied rigorous tokenization via strict base-4 spacing scale (`w-48`, `min-h-20`) and HSL-based semantic tokens across CSS files.
