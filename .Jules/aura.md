# Aura's Design System Journal

## 2024-04-19 - [Initialization] **Learning:** Established baseline semantic HSL tokens to replace legacy hardcoded hex values (`--bg-primary`, `--text-primary`, etc.). **Action:** Ensure all `.module.css` files are updated to strictly use `hsl(var(...))` mapped standard variables.
