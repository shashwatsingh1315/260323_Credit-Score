## 2024-04-08 - [CSS Modules Hardcoded Colors] **Learning:** Legacy UI patterns frequently use `rgba()` for semi-transparent backgrounds and interactive states (e.g., hover effects, warning states), ignoring light/dark mode and semantic design tokens. **Action:** Refactor all `rgba(r, g, b, alpha)` occurrences in CSS modules to use `hsl(var(--semantic-token) / alpha)` syntax to respect global theme tokens.

## 2024-04-08 - [Arbitrary Dimensions in Shell] **Learning:** The main layout shell (`src/components/Shell.module.css`) uses hardcoded arbitrary numbers for structure (`width: 260px`, `height: 70px`, `width: 32px`). **Action:** These numbers should map to a consistent, tokenized base-8/base-4 scale or be established as layout variables to align with the Aura philosophy of strict uniformity.